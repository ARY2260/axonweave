# Security Policy

Do not report security vulnerabilities publicly before maintainers have had an opportunity to assess them.

Never commit API keys, PyPI tokens, cloud credentials, private URLs or user datasets.

The documentation site must not contain client-side secrets. Analytics identifiers, if used, must be configured through deployment environment variables or a privacy-preserving provider.
